# PostgreSQL (VM1 internal IP)
DATABASE_URL=postgresql://manike:manike_secret@<POSTGRES_INTERNAL_IP>:5432/manike_db
POSTGRES_PASSWORD=manike_secret

# Milvus (VM2 internal IP)
MILVUS_HOST=<MILVUS_INTERNAL_IP>
MILVUS_PORT=19530

# GCS (bucket is publicly readable; no signed URLs)
GCS_BUCKET_NAME=manike-ai-media
GCS_BASE_PREFIX=experience-images

# Auth
SECRET_KEY=change-me-to-a-random-string

# AI Provider (gemini or claude)
AI_PROVIDER=gemini
GEMINI_API_KEY=<your-gemini-api-key>
GEMINI_MODEL=gemini-2.5-flash
# CLAUDE_API_KEY=<your-claude-api-key>
# CLAUDE_MODEL=claude-sonnet-4-6

# Video Compiler (local or cloudrun)
VIDEO_COMPILER=local
# CLOUD_RUN_JOB_NAME=projects/<project>/locations/<region>/jobs/manike-video-compiler
# CLOUD_RUN_REGION=us-central1
